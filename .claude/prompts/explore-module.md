Explore module `<module-name>`.

Map and summarise:
1. **Entry points** — what callers invoke this module.
2. **Public API surface** — exported functions / classes / types.
3. **Internal structure** — main files and their responsibilities.
4. **Dependencies** — both within the repo and external packages.
5. **Test coverage** — which behaviours are tested, which aren't.

Output as a 1-page brief. Use a tree diagram for structure.
