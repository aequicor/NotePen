package ru.kyamshanov.notepen.reflow.ui

/**
 * Текущий локальный час `0..23`. Платформенно (через `expect/actual`), без новых
 * зависимостей — нужен для опции «теплеть после заката»: точное время заката
 * зависит от геопозиции, поэтому используем приближение по вечерним часам.
 */
internal expect fun currentLocalHour(): Int
