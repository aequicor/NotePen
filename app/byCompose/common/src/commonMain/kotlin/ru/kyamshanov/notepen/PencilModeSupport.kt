package ru.kyamshanov.notepen

/**
 * `true` на платформах, где имеет смысл показывать переключатель Pencil Mode —
 * то есть там, где основным указателем может быть палец, и нужно явно
 * запретить рисование пальцем в пользу стилуса. На десктопе всегда `false`:
 * мышь не «палец», и palm-rejection сломал бы рисование основной мышью.
 */
expect val SupportsPencilMode: Boolean
