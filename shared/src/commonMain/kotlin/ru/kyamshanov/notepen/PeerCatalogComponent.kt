package ru.kyamshanov.notepen

/**
 * Контракт Decompose-компонента sub-экрана каталога одного пира.
 *
 * Объявляется в `:shared`, реализуется в `:common` через
 * `ru.kyamshanov.notepen.mainscreen.ui.peer.PeerCatalogComponentImpl` —
 * `:shared` не зависит от `:common`.
 */
interface PeerCatalogComponent
