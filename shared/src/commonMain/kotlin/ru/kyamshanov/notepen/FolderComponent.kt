package ru.kyamshanov.notepen

/**
 * Контракт Decompose-компонента sub-экрана содержимого папки.
 *
 * Объявляется в `:shared`, реализуется в `:common` через
 * `ru.kyamshanov.notepen.mainscreen.ui.folder.FolderContentsComponentImpl` —
 * `:shared` не зависит от `:common`.
 */
interface FolderComponent
